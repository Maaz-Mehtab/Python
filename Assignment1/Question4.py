# Q4)Write a Python program which accepts the radius of a circle from the user
# and compute the area.

from math import pi
radius = 2
print (" radius of a circle " + str(radius) + " is: " + str(pi * radius**2))